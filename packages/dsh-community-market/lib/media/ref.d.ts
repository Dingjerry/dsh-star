export declare const MARKET_MEDIA_ASSET_REF_PATTERN: RegExp;
/** Convert an opaque Host-issued reference into the only renderer-facing asset URL. */
export declare function marketMediaAssetUrl(assetRef: string): string;
